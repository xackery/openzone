
{$DEFINE NO_SOFTWARE_PREFETCHING}       // remove this to enable software prefetching (useful on P-III machines)
{$DEFINE CISC_STYLE}                    // enables op(reg1,mem) instructions instead of load(reg2,mem)/op(reg1,reg2) couplets







